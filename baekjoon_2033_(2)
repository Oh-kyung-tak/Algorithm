#include<iostream>
#include<set>
#include<algorithm>
#include<string>

using namespace std;

int main() 
{
	int num;
	int s = 1;

	scanf("%d", &num);

	while (num > s * 10)
	{
		num += 5 * s;
		s *= 10;
		num -= num % s;
	}

	printf("%d", num);
}
